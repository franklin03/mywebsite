<h1>Modify Name "<%= @user.name %> "</h1>

<%= form_with(model: @user, local: true) do |f| %>
  <%= f.label :name, 'name' %>
  <%= f.text_field :name %>
  <%= f.submit 'edit' %>
<% end %>

<%= link_to 'back', user_path %>